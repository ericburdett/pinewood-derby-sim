"""Pinewood Derby Simulator — pure simulation engine.

This package holds the physics engine only (no UI, no I/O). Features are built by the
SDLC factory's build loop; this is an empty, dependency-free starting point.
"""

__version__ = "0.0.0"
